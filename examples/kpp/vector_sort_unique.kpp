#포함 <algorithm>
#포함 <iostream>
#포함 <vector>

사용 범위 표준;

정수 진입() {
  벡터<정수> v = {4, 2, 2, 8, 1, 4};

  정렬(v.시작(), v.끝());
  자동 last = 중복제거(v.시작(), v.끝());
  v.erase(last, v.끝());

  출력 << "정렬 + 중복제거 결과: ";
  반복 (정수 x : v) {
    출력 << x << ' ';
  }
  출력 << 끝줄;

  반환 0;
}

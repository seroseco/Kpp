#포함 <iostream>

사용 범위 표준;

정수 진입() {
  정수 n;
  출력 << "N 입력: ";
  입력 >> n;

  반복 (정수 i = 1; i <= n; ++i) {
    만약 (i % 2 == 0) {
      출력 << i << " 는 짝수" << '\n';
    } 아니면 {
      출력 << i << " 는 홀수" << '\n';
    }
  }

  반환 0;
}

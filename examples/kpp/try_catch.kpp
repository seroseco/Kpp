#포함 <exception>
#포함 <iostream>

사용 범위 표준;

정수 나누기(정수 a, 정수 b) {
  만약 (b == 0) {
    던지기 runtime_error("0으로 나눌 수 없습니다");
  }
  반환 a / b;
}

정수 진입() {
  시도 {
    출력 << 나누기(10, 2) << '\n';
    출력 << 나누기(10, 0) << '\n';
  } 잡기 (const exception& e) {
    출력 << "예외: " << e.what() << '\n';
  }

  반환 0;
}

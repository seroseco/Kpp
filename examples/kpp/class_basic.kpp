#포함 <iostream>
#포함 <string>

사용 범위 표준;

클래스 사용자 {
공개:
  문자열 이름;
  정수 나이;

  사용자(문자열 n, 정수 a) : 이름(n), 나이(a) {}

  빈 소개() {
    출력 << "이름: " << 이름 << ", 나이: " << 나이 << '\n';
  }
};

정수 진입() {
  사용자 u("Kim", 20);
  u.소개();
  반환 0;
}

#포함 <iostream>
#포함 <map>
#포함 <string>

사용 범위 표준;

정수 진입() {
  맵<문자열, 정수> m;
  m["apple"] = 2;
  m["banana"] = 5;
  m["apple"] += 3;

  출력 << "apple: " << m["apple"] << '\n';
  출력 << "banana: " << m["banana"] << '\n';
  출력 << "키 개수: " << m.크기() << '\n';

  반환 0;
}
